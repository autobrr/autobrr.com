import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>

<TabItem value="seedboxio" label="Hostingby.design">

```shell
wget https://gobrr.sh/install_sbio && bash install_sbio
```

Previously known as seedbox.io. Same installer works.

</TabItem>

<TabItem value="swizzinnet" label="Swizzin.net">

```shell
wget https://gobrr.sh/install_sbio && bash install_sbio
```

Same installer as Hostingby.design

</TabItem>

<TabItem value="ultra" label="Ultra.cc">

```shell
wget https://gobrr.sh/install_ultra && bash install_ultra
```

</TabItem>

<TabItem value="whatbox" label="WhatBox">

```shell
wget https://gobrr.sh/install_whatbox && bash install_whatbox
```

</TabItem>

<TabItem value="feral" label="Feralhosting">

```shell
wget https://gobrr.sh/install_feral && bash install_feral
```

</TabItem>

<TabItem value="bytesized" label="Bytesized Hosting">

```shell
wget https://gobrr.sh/install_bytesized && bash install_bytesized
```

</TabItem>

<TabItem value="other" label="Other provider">

```shell
wget https://gobrr.sh/install_sbio && bash install_sbio
```

The seedbox.io installer might work for other providers, but if not reach out in Discord.

</TabItem>

</Tabs>
