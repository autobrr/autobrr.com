import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>
<TabItem value="qbittorrent" label="qBittorrent">

```
Host: Takes an IP or URL without http(s), eg. myserver.test.
Port: <WebUI port>
SSL: Use this if the server is on https. It's usually not the case.
Username: <username>
Password: <password>
```

</TabItem>

<TabItem value="deluge" label="Deluge">

```
Host: Takes an IP or URL without http(s), eg. myserver.test.
Port: <daemon port>
SSL: Use this if the server is on https. It's usually not the case.
Username: <username>
Password: <password>
```

</TabItem>

<TabItem value="arr suite" label="*arr suite">

```
Host: Takes an IP or URL with http(s), eg http://server.test/radarr
API key: <API key>
Basic auth: Disabled by default.
Username: <username>
Password: <password>

Some setups need basic auth, others don't. Try without first.
```

</TabItem>

</Tabs>
